#ifndef __CRITERION_ARGS_FLAGS_LIST_H__
    #define __CRITERION_ARGS_FLAGS_LIST_H__

int is_makefile_flag(const char *flag);
int is_help_flag(const char *flag);
int is_folder_flag(const char *flag);

#endif